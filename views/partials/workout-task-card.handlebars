{{!-- /* @ To Do
* change background-color and styles
* change layout
* change to card or something esle
* change button
* change from inline css style to class or id...
*/ --}}
{{!--==================== High ====================--}}
{{#ifCond intensify "high"}}
{{#if isFinished}}
<div style="background-color: #f7b7bb; text-decoration-line: line-through;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{else}}
<div style="background-color: #ff6b6b;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/if}}
{{/ifCond}}
{{!-- {{#ifCond intensify "high"}}
<div style="background-color: #ff6b6b;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/ifCond}} --}}


{{!--==================== Moderate ====================--}}
{{#ifCond intensify "moderate"}}
{{#if isFinished}}
<div style="background-color: lightgreen; text-decoration-line: line-through;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{else}}
<div style="background-color: #2bb41a;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/if}}
{{/ifCond}}

{{!--==================== Low ====================--}}
{{#ifCond intensify "low"}}
{{#if isFinished}}
<div style="background-color: #D0ECEF; text-decoration-line: line-through;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{else}}
<div style="background-color: #2FADCC;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/if}}
{{/ifCond}}


{{!-- <div>
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{date_created}}
    </div>
</div> --}}