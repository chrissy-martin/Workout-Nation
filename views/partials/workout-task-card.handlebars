{{!-- <div>
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{date_created}}
    </div>
</div> --}}

{{!--==================== High ====================--}}
{{#ifCond intensify "high"}}
{{#if isFinished}}
<div style="background-color: #f7b7bb;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{else}}
<div style="background-color: #ff6b6b;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/if}}
{{/ifCond}}
{{!-- {{#ifCond intensify "high"}}
<div style="background-color: #ff6b6b;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/ifCond}} --}}


{{!--==================== Moderate ====================--}}
{{#ifCond intensify "moderate"}}
{{#if isFinished}}
<div style="background-color: lightgreen;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{else}}
<div style="background-color: #2bb41a;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/if}}
{{/ifCond}}
{{!-- {{#ifCond intensify "moderate"}}
<div style="background-color: lightgreen;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button id="setFinish">👌</button>
    <button id="deleteTask">🗑️</button>
</div>
{{/ifCond}} --}}

{{!--==================== Low ====================--}}

{{#ifCond intensify "low"}}
{{#if isFinished}}
<div style="background-color: #D0ECEF;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{else}}
<div style="background-color: #2FADCC;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button class="setFinish">👌</button>
    <button class="undo">↩️</button>
    <button class="deleteTask">🗑️</button>
</div>
{{/if}}
{{/ifCond}}
{{!-- {{#if isFinished}}
<div style="background-color: #D0ECEF;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button id="setFinish">👌</button>
    <button id="undo">↩️</button>
    <button id="deleteTask">🗑️</button>
</div>
{{else}}
{{#ifCond intensify "low"}}
<div style="background-color: #2FADCC;" class="aTask" data-taskid="{{id}}">
    <div>
        <h2>{{taskname}} ({{intensify}})</h2>
        <h2>Durition: {{duration_minute}} min</h2>
    </div>
    <div>
        Posted by <cite title="username">{{user.username}}</cite> on {{format_date date_created}}
    </div>
    <button id="setFinish">👌</button>
    <button id="undo">↩️</button>
    <button id="deleteTask">🗑️</button>
</div>
{{/ifCond}}
{{/if}} --}}